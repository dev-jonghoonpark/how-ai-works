# AI 용어 사전

자료를 읽다 막히는 용어를 정리합니다.

---

## 텐서 (Tensor)

**숫자를 담는 다차원 배열.** 스칼라·벡터·행렬을 하나의 이름으로 묶은 것이라고 보면 됩니다.

| 차수(rank) | 이름 | 예시 | shape |
| --- | --- | --- | --- |
| 0 | 스칼라 | 손실값 `2.31` | `()` |
| 1 | 벡터 | 단어 임베딩 | `(300,)` |
| 2 | 행렬 | 완전연결층 가중치 | `(512, 300)` |
| 3 | 텐서 | RGB 이미지 한 장 | `(3, 224, 224)` |
| 4 | 텐서 | 이미지 배치 | `(32, 3, 224, 224)` |

차수는 "축이 몇 개인가"이고, shape는 "각 축의 길이가 얼마인가"입니다. 위 4차원 예시는 `(배치, 채널, 높이, 너비)` 순서로, 224×224 RGB 이미지 32장을 한 덩어리로 묶은 것입니다.

### 왜 이런 이름이 붙었나

물리·수학에서 말하는 텐서는 좌표계를 바꿔도 정해진 규칙대로 변환되는 양을 뜻합니다. 딥러닝에서 쓰는 "텐서"는 그 조건까지 요구하지 않고, 사실상 **다차원 배열**을 부르는 말입니다. 이름만 빌려 온 셈이라 수학 쪽 정의를 먼저 찾아보면 오히려 헷갈립니다.

### 실제로 중요한 이유

- **GPU가 좋아하는 모양이라서.** 값을 하나씩 처리하는 대신 축 단위로 묶어 한 번에 계산합니다. 이미지를 32장씩 묶어 배치로 넣는 이유도 이것입니다.
- **디버깅의 절반이 shape 맞추기라서.** 신경망 코드에서 가장 자주 만나는 에러는 수학이 틀린 게 아니라 축의 길이가 안 맞는 경우입니다. 층을 하나 지날 때마다 shape가 어떻게 변하는지 따라가는 습관이 곧 모델을 이해하는 습관입니다.

### 자주 하는 연산

- **reshape / view** — 값은 그대로 두고 축만 다시 나눕니다. `(32, 3, 224, 224)` → `(32, 150528)`
- **transpose / permute** — 축의 순서를 바꿉니다. `(N, C, H, W)` ↔ `(N, H, W, C)` (PyTorch와 TensorFlow의 기본 순서가 다릅니다)
- **브로드캐스팅** — 길이가 1인 축을 자동으로 늘려 서로 다른 shape끼리 계산되게 합니다. `(32, 300) + (300,)`
- **행렬 곱** — 앞 텐서의 마지막 축과 뒤 텐서의 첫 축 길이가 같아야 합니다. `(32, 300) × (300, 512)` → `(32, 512)`

> 관련 자료: [How Convolution Works](https://github.com/dev-jonghoonpark/how-conv-work) — 채널 축이 실제로 어떻게 늘고 줄어드는지 직접 계산해 볼 수 있습니다.

---

## 단방향 RNN / 양방향 RNN (Unidirectional / Bidirectional RNN)

**시간 축을 한 방향으로만 읽느냐, 앞뒤 양쪽에서 읽느냐의 차이.** 셀(RNN·LSTM·GRU)은 그대로고, 바뀌는 것은 **은닉 상태를 어느 방향으로 나르는가** 하나뿐입니다.

```
단방향   x₁ → x₂ → x₃ → x₄ → x₅        h_t = f(x_t, h_{t−1})
          ──────────────────▶           t 시점 출력은 1..t 만 봤다

양방향   x₁ → x₂ → x₃ → x₄ → x₅        h_t⁽ᶠ⁾ = f(x_t, h_{t−1}⁽ᶠ⁾)
          ──────────────────▶
         x₁ ← x₂ ← x₃ ← x₄ ← x₅        h_t⁽ᵇ⁾ = f(x_t, h_{t+1}⁽ᵇ⁾)
          ◀──────────────────
                                        h_t = h_t⁽ᶠ⁾ ⊕ h_t⁽ᵇ⁾
```

양방향은 **독립적인 RNN 두 개**를 반대 방향으로 돌린 뒤 같은 시각의 출력을 합칩니다(Schuster & Paliwal, 1997). 합치는 방법은 구현마다 다릅니다 — PyTorch `nn.LSTM(bidirectional=True)`은 **이어 붙이기(concat)**라 출력 차원이 2배가 되고, Deep Speech 2는 **더하기(sum)**라 차원이 그대로입니다. 두 방향의 은닉 상태가 서로를 참조하지는 않습니다(그러면 순환이 생겨 계산할 수 없습니다).

| | 단방향 | 양방향 |
| --- | --- | --- |
| t 시점이 보는 범위 | 과거 전부 (1..t) | **시퀀스 전체** (1..T) |
| 첫 출력이 나오는 시점 | 입력 한 칸이 들어오는 즉시 | **입력이 전부 끝난 뒤** |
| 지연(latency) | 없음 (스트리밍 가능) | 시퀀스 길이에 비례 |
| 파라미터·계산량 | 1배 | 2배 |
| 쓸 수 있는 곳 | 실시간 음성 인식, 언어 모델, 생성 | 오프라인 음성 인식, 태깅, 분류, 인코더 |

### 양방향이 더 정확한 이유

시퀀스에서 **판별 근거가 뒤에 오는** 경우가 흔하기 때문입니다.

- 음성: 영어 `/t/`와 `/d/`는 파열 순간만으로는 구분이 잘 안 되고, **뒤따르는 모음의 성대 진동 시작 시점(VOT)**을 봐야 갈립니다. 단방향 모델은 그 증거가 도착하기 전에 이미 답을 내야 합니다.
- 텍스트: `"bank"`가 은행인지 강둑인지는 뒷 단어를 봐야 알고, 한국어 `"감사합니다"`의 `"감사"`도 마찬가지입니다.

CTC 학습에서 특히 그렇습니다. CTC는 정렬을 스스로 찾는데, 뒤를 볼 수 있으면 "이 프레임이 어느 글자에 속하는가"를 훨씬 뾰족하게 정할 수 있습니다.

### 자주 하는 오해

- **"양방향은 미래를 예측한다"** — 아닙니다. 이미 **다 받아 놓은** 시퀀스를 뒤에서부터 한 번 더 읽을 뿐입니다. 아직 오지 않은 입력을 만들어 내는 것이 아닙니다.
- **"그럼 언어 모델도 양방향으로 만들면 되지 않나"** — 다음 토큰을 맞히는 과제에서는 쓸 수 없습니다. 역방향 패스가 **정답 토큰 자체를 이미 읽어 버리기** 때문에 라벨 누수가 됩니다. 같은 이유로 Transformer는 causal mask로 미래를 가립니다. BERT(양방향 인코더)가 다음 토큰 예측 대신 마스킹 복원(MLM)으로 학습하는 것도 이 때문입니다.
- **"양방향이면 기울기 소멸이 덜하다"** — 무관합니다. 각 방향은 여전히 자기 방향으로 T스텝을 통과합니다. 그건 LSTM/GRU가 푸는 문제입니다.

### Deep Speech 2 — 단방향으로 되돌리기 위해 붙이는 층

DS2가 이 개념의 교과서적인 사례입니다. 논문이 자랑하는 최고 정확도 모델과 실제 서비스에 올린 모델이 **여기서 갈립니다**.

| | 연구용 시스템 | 배포 시스템 |
| --- | --- | --- |
| 순환층 | **양방향 7층** | **단방향 5층(2560) + 행 합성곱 τ=19** |
| 지연 | 발화 길이에 비례 | 상수 (τ·스텝 = 19 × 40 ms = **760 ms**) |
| 중국어 CER (2,000 발화) | 5.81 | 6.10 |

양방향 순환층의 역방향 패스는 **마지막 프레임에서 시작**합니다. 그래서 사용자가 말을 끝내기 전에는 첫 글자조차 낼 수 없습니다. 실시간 서비스에서는 치명적이라, DS2 배포 모델은 순환층을 **전부 단방향으로** 바꿉니다.

그런데 그냥 바꾸면 위에서 본 "뒤를 봐야 갈리는" 증거를 잃어 정확도가 떨어집니다. 그래서 순환 스택 **맨 위에 딱 한 층**, 미래 τ 스텝만 훔쳐보는 층을 얹습니다 — 이것이 **행 합성곱(row convolution)**, 논문 3.7절 식 (11)입니다. 같은 구조가 **lookahead convolution**이라는 이름으로도 불립니다(Wang et al., 2016).

```
r[t,i] = Σ_j W[i,j] · h[t+j−1, i]        (식 11)
```

핵심은 **`i`가 양변에 그대로 있다**는 것입니다. 채널 `i`는 자기 채널의 미래 τ 칸만 보고, 채널끼리 섞지 않습니다(= depthwise 1D convolution). 채널을 섞었다면 파라미터가 정확히 `d`배(2,560배) 늘어 순환 스택 전체보다 커집니다.

여기서 꼭 붙잡고 갈 것:

- **행 합성곱은 단방향 RNN을 쓸 때만 추가되는 층입니다.** 양방향 모델에는 없습니다 — 이미 미래를 끝까지 보고 있으니 덧붙일 이유가 없습니다. "양방향의 미래 문맥"을 **유한한 τ개로 잘라 흉내 내는** 부품입니다.
- 이 교체로 지연이 **발화 길이 비례 → 상수**로 바뀝니다. 배포 시스템의 실제 지연은 중앙값 44 ms, 98백분위 67 ms입니다.
- 정확도 손실은 상대 5%(CER 5.81 → 6.10)에 그쳤고, **중국어에서는 이 근사가 최고의 양방향 모델보다 CER이 낮게 나온 경우도 있었습니다.**
- 같은 아이디어가 지금도 쓰입니다. Conformer·Transformer 기반 스트리밍 모델의 **청크 단위 어텐션**, RNN-T의 **right context**가 "미래를 상수만큼만 본다"는 같은 맞교환입니다.

> 관련 자료
> - [How RNN Works](https://github.com/dev-jonghoonpark/how-rnn-works) — 순환층이 시간 축으로 상태를 나르는 기본기
> - [How Bidirectional RNN Works](https://github.com/dev-jonghoonpark/how-bidirectional-rnn-works) — 이 항목을 한 편으로 풀어낸 자료. 시간 전개 재생기·의존성 행렬·증거 도착 실험·파라미터 계산기
> - [How Row Convolution Works](https://github.com/dev-jonghoonpark/how-row-convolution-works) — 세 구조(양방향·단방향·단방향+행 합성곱)를 실시간 재생으로 비교하고, 식 (11)을 칸 클릭으로 전개해 봅니다
> - [How Deep Speech 2 Works](https://github.com/dev-jonghoonpark/how-deep-speech-2-works) — 연구용 모델과 배포 모델이 갈리는 지점을 논문 전체 맥락에서
> - [How Attention Works](https://github.com/dev-jonghoonpark/how-attention-works) — causal mask가 Transformer에서 같은 역할을 합니다

---

## GRU (Gated Recurrent Unit)

**LSTM을 단순화한 게이트 RNN 셀.** Cho 외, 「Learning Phrase Representations using RNN Encoder–Decoder for Statistical Machine Translation」(EMNLP 2014)이 seq2seq 번역기의 은닉 유닛으로 제안했고, GRU라는 이름은 같은 해 Chung 외의 비교 논문에서 붙었습니다. 긴 시퀀스에서 기울기가 사라지는 문제를 **게이트로 푼다**는 아이디어는 LSTM과 같고, 다른 것은 구조의 가벼움입니다.

| | LSTM | GRU |
| --- | --- | --- |
| 게이트 | 3개 (입력·망각·출력) | **2개 (리셋·업데이트)** |
| 상태 | 셀 상태 `c_t` + 은닉 상태 `h_t` | **은닉 상태 `h_t` 하나** |
| 가중치 묶음 (게이트 + 후보) | 4벌 | **3벌** |
| 파라미터 (`d_x = d_h = 256`, PyTorch) | 526,336 | **394,752** (정확히 3/4) |
| 출력 | `o_t ⊙ tanh(c_t)` — 일부만 내보냄 | `h_t` 그대로 — 상태 전체가 곧 출력 |

### 식

```
z_t = σ(W_z x_t + U_z h_{t−1} + b_z)              업데이트 게이트
r_t = σ(W_r x_t + U_r h_{t−1} + b_r)              리셋 게이트
h̃_t = tanh(W_h x_t + U_h (r_t ⊙ h_{t−1}) + b_h)   후보
h_t = (1 − z_t) ⊙ h_{t−1} + z_t ⊙ h̃_t             섞기
```

- **업데이트 게이트 `z`** — "이전 정보를 얼마나 유지하고 새 정보를 얼마나 받을지"를 **한 번에** 정합니다. 유지하는 몫과 받는 몫이 `1 − z`와 `z`로 묶여 있어 합이 항상 1입니다. LSTM의 망각 게이트 `f`와 입력 게이트 `i`를 **`f = 1 − i`로 묶은 것**과 같습니다.
- **리셋 게이트 `r`** — "새 후보를 만들 때 과거를 얼마나 무시할지"를 정합니다. `r ≈ 0`이면 후보 `h̃`가 이전 상태를 보지 않고 현재 입력만으로 만들어져, 문장 경계처럼 앞 맥락을 끊어야 하는 지점에서 쓰입니다.
- **출력 게이트는 없습니다.** LSTM은 셀 상태를 안쪽에 감춰 두고 일부만 꺼내 보여 주지만, GRU는 기억하는 것과 내보내는 것이 같은 벡터입니다.

### 기울기가 사라지지 않는 이유

`z_t ≈ 0`이면 `h_t ≈ h_{t−1}`이 되어 `∂h_t / ∂h_{t−1} ≈ I`입니다. 행렬을 곱하고 tanh를 통과하는 대신 **거의 그대로 복사되는 길**이 생기는 것이고, LSTM에서 `f ≈ 1`일 때 셀 상태 레일이 하는 일과 같습니다. 바닐라 RNN은 매 스텝 `U`와 tanh 미분을 곱해 기울기가 `λ^k`로 줄어들지만, GRU는 그 곱셈을 **건너뛸지 말지를 게이트가 학습**합니다.

### 무엇을 잃는가

`1 − z`와 `z`의 합이 1이라 `h_t`는 이전 상태와 후보의 **볼록 결합**입니다. 그래서 `h_t`는 항상 `(−1, 1)` 안에 머뭅니다. LSTM은 `f = 1`, `i = 1`로 **지우지 않고 계속 더할 수 있어** 셀 상태가 경계 없이 자라는데, GRU는 새 것을 받는 만큼 옛것을 덜어 내야 합니다. 이 차이 때문에 LSTM은 `aⁿbⁿ`처럼 개수를 세야 하는 과제를 풀 수 있고 GRU는 못 푼다는 것이 보고되었습니다(Weiss 외, ACL 2018).

실전 성능은 **대체로 비슷합니다.** Chung 외(2014)는 과제에 따라 승자가 갈려 우열을 가리지 못했고, Jozefowicz 외(ICML 2015)는 GRU가 앞서는 과제를 찾았지만 LSTM 망각 게이트 편향을 1로 초기화하면 격차가 대부분 사라진다고 보고했습니다. Greff 외, 「LSTM: A Search Space Odyssey」(2017)도 입력·망각 게이트를 묶은 변형(CIFG — GRU의 핵심 아이디어)이 성능을 크게 해치지 않는다고 확인했습니다. 그래서 선택 기준은 대개 **파라미터·속도 대비 성능**입니다.

### 자주 하는 오해

- **"`z`가 1이면 유지다"** — 구현마다 반대입니다. Cho 외 원 논문은 `h_t = z ⊙ h_{t−1} + (1 − z) ⊙ h̃_t`로 `z`가 **유지하는 몫**이고, 위 식(Chung 외 표기)은 `z`가 **받는 몫**입니다. PyTorch `nn.GRU`는 원 논문 쪽입니다. 게이트 값을 시각화해 해석할 때 먼저 확인해야 합니다.
- **"리셋 게이트는 `h_{t−1}`에 곱한다"** — 원 논문은 그렇지만, PyTorch·cuDNN과 Keras 기본값(`reset_after=True`)은 **행렬 곱 뒤에** 곱합니다: `h̃_t = tanh(W_h x_t + r_t ⊙ (U_h h_{t−1} + b_hn))`. 이렇게 하면 `U_z h`, `U_r h`, `U_h h`를 **GEMM 한 번**으로 몰아 계산할 수 있어 빠릅니다. Deep Speech 2(3.4절)가 쓴 변형이 바로 이것입니다. 두 방식은 가중치 호환이 안 되므로 프레임워크 간에 GRU 가중치를 옮길 때 자주 걸립니다.
- **"GRU는 LSTM의 개선판이다"** — 나중에 나온 **단순화판**입니다. 표현력은 오히려 LSTM이 약간 넓고(위의 세기 문제), GRU가 주는 것은 3/4의 파라미터와 더 적은 연산입니다.

> 관련 자료
> - [How GRU Works](https://github.com/dev-jonghoonpark/how-gru-works) — 이 항목을 한 편으로 풀어낸 자료. 수술 단계 뷰어·합류 실험·카운터·브라우저 학습 비교
> - [How LSTM Works](https://github.com/dev-jonghoonpark/how-lstm-works) — 셀 상태 레일과 게이트 조작기, 결합 게이트·GRU 변형과 파라미터 비교(LSTM : GRU : 바닐라 = 4 : 3 : 1)
> - [How RNN Works](https://github.com/dev-jonghoonpark/how-rnn-works) — 게이트가 없을 때 무엇이 문제인지부터
> - [How Deep Speech 2 Works](https://github.com/dev-jonghoonpark/how-deep-speech-2-works) — `h(t−1)`에 걸리는 GEMM 3개가 1개로 합쳐지는 GRU 변형과, 1억 파라미터에서 바닐라 RNN에 뒤집히는 표 11
> - [DS2 My Voice](https://github.com/dev-jonghoonpark/ds2-my-voice) — 양방향 GRU 5층을 PyTorch로 직접 학습시켜 보는 실습

---

## Post-LN / Pre-LN (잔차 블록에서 정규화를 어디에 두는가)

**잔차 연결과 정규화 층의 순서.** 괄호 위치 하나 차이지만, 깊게 쌓았을 때 학습이 되느냐 마느냐가 여기서 갈립니다.

```
Post-LN (Attention Is All You Need, 2017)      Pre-LN (오늘날의 표준)

  x ──┬─────────────────┐                        x ──┬──────────────────┐
      │                 │                            │                  │
      └─▶ Sublayer ─────┼─▶ (+) ─▶ LayerNorm ─▶      └─▶ LayerNorm ─▶ Sublayer ─▶ (+) ─▶
                        │           ▲                                       ▲
                     잔차가 매 층 LN을 통과                          잔차 경로가 뚫려 있다
```

| | Post-LN | Pre-LN |
| --- | --- | --- |
| 식 | `LayerNorm(x + F(x))` | `x + F(LayerNorm(x))` |
| 잔차 스트림 크기 | 매 층 `√d`로 리셋 | **층마다 누적 — `√N`에 비례해 자람** |
| 가지/줄기 비율 `‖F(x)‖/‖x‖` | 깊이와 무관하게 **일정** | **`1/√ℓ`로 감소** |
| 깊게 쌓으면 | 층 하나의 영향력이 그대로 → 초기에 불안정 | 층이 저절로 항등에 가까워짐 → 안정 |
| 워밍업 | **필수** (논문 4000스텝) | 없어도 학습됨 |
| 추가로 필요한 것 | — | 마지막 블록 뒤 `final_layer_norm` 하나 |

### 왜 이런 차이가 나는가

Post-LN은 LN이 **맨 마지막**에 있어 잔차 스트림 크기를 매 층 되돌려 놓습니다. 그래서 각 층의 서브층 출력이 스트림의 **일정 비율을 계속 덮어씁니다** — 층을 아무리 쌓아도 층 하나의 상대적 영향력이 줄지 않고, 초기 상태가 조금만 어긋나면 위로 갈수록 증폭됩니다. 원 논문이 학습률을 4000스텝에 걸쳐 천천히 올린 이유가 이것입니다.

Pre-LN은 반대입니다. 줄기는 더해지기만 해서 `√N`으로 자라는데 가지 출력은 입력을 먼저 LN으로 눌러 놓고 계산하므로 항상 `O(1)`입니다. 비율이 `1/√ℓ`로 떨어지니 **깊게 쌓을수록 각 층이 저절로 항등에 가까워집니다.** Xiong 외, 「On Layer Normalization in the Transformer Architecture」(ICML 2020)가 초기화 시점 기울기 크기로 이를 정리하고, Pre-LN에서는 워밍업을 제거할 수 있음을 실험으로 확인했습니다.

### 자주 하는 오해

- **"Pre-LN이 무조건 낫다"** — 잔차 스트림이 정규화되지 않은 채 계속 자라므로 마지막에 LN을 하나 더 붙여야 합니다. 같은 층 수·같은 학습 예산에서 Post-LN이 더 좋은 점수를 내는 경우도 보고됩니다.
- **"LayerNorm은 BatchNorm의 변형이라 배치가 필요하다"** — 아닙니다. LN은 **한 샘플 한 토큰**의 `d`개 차원만으로 평균·분산을 냅니다. 배치와 무관하고, 길이가 제각각인 시퀀스에서도 문제가 없으며, 추론용 이동평균도 필요 없습니다. BN과 수식은 같고 **축만 다릅니다.**
- **"ResNet의 v1/v2와는 다른 이야기"** — 같은 이야기입니다. ResNet v2의 pre-activation이 정확히 이 "정규화를 가지 안쪽으로 옮기기"입니다.

> 관련 자료
> - [How Transformer Works](https://github.com/dev-jonghoonpark/how-transformer-works) — 브라우저가 실제로 순전파해 가지/줄기 비율이 갈리는 것을 보여 주는 §5
> - [How ResNet Works](https://github.com/dev-jonghoonpark/how-resnet-work) · [ResNet DJL Lab](https://github.com/dev-jonghoonpark/resnet-djl-lab) — BN·ReLU·덧셈의 순서 하나가 v1과 v2를 가른다
> - [How Mean and Variance Works](https://github.com/dev-jonghoonpark/how-mean-and-variance-works) — BN·LN·IN·GN이 같은 수식이고 축만 다르다는 것을 칸 클릭으로
> - [ResNet의 BatchNorm](https://github.com/dev-jonghoonpark/resnet-batchnorm) — 깊은 네트워크에 정규화가 왜 필요한가
